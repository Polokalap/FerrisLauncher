import { Sidebar } from "sidebar/Sidebar.slint";
export { AccountMenuState } from "state.slint";
export { SidebarState } from "state.slint";

export component Window inherits Window {
    width: 1200px;
    height: 700px;
    background: #1e1e1e;
    title: "Ferris Launcher";

    Sidebar {
        height: 712px;
        width: 1200px;
        x: -0px;
    }

}
