export global AccountMenuState {
    in-out property <image> head-image: @image-url("../static/heads/unknown.png");
    in-out property <string> player-name: "Unknown";
}

export global SidebarState {
    in-out property <bool> is_open: false;
}